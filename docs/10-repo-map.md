# Repo map

- `tiers/` : découpage 3 tiers (Edge/API, webMethods, Kafka/Streaming)
- `contracts/` : contrats d’événements (schemas + exemples)
- `docs/` : architecture, NFR/SLO, MCO, perf
- `ops/` : runbooks + observabilité (placeholders)
- `ci/` : gabarits Jenkins/GitLab CI
- `ansible/` : automatisation (squelette)
- `scripts/` : scripts bash idempotents
- `local-dev/` : docker-compose Kafka local
