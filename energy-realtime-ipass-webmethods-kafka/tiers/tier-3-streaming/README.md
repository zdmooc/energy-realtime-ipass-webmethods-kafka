# Tier 3 — Streaming / Fast Data (Kafka)

Objectif: gouverner et opérer Kafka (topics, DLQ, replay), et fournir un socle streaming.

## Dossiers
- `kafka/topics/` : définitions topics (exemples)
- `kafka/acls/` : ACLs (placeholders)
- `streams/` : exemple app Kafka Streams (squelette)
- `consumers/` : exemple consommateur (python)
